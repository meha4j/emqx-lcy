extd:
  port: 9111
  emqx:
    host: emqx
    port: 18083
    rmax: 5
    tout: 15s
  pgsql:
    host: pgsql
    port: 5432
  proc:
    server:
      port: 9110
    name: vcas
    tout: 30s
    enable: false
    listener:
      name: default
      type: tcp
      port: 20041
  auth:
    name: extd
    tout: 30s
    trec: 60s
    enable: false
    action: deny
    pgsql:
      name: postgres
      query: SELECT top FROM rule WHERE mod = 'ex'
